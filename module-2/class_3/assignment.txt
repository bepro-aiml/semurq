# Module 2 | Class 3 Assignment: Data Visualization and Trend Analysis

**Related slides:** Class 3 — Data Visualization: Matplotlib and Seaborn
**Estimated time:** 60 minutes

## Objective
Create meaningful visualizations from the cleaned Superstore dataset and interpret the patterns you find. A chart without interpretation is just a picture.

## What You Need
- Tool: Google Colab: https://colab.research.google.com
- Dataset: Your cleaned Superstore CSV from the Class 2 assignment (`superstore_cleaned.csv`)
- Libraries: `pandas`, `matplotlib`, `seaborn` (all pre-installed in Colab)

## Tasks

### Task 1: Distribution Plots
**What to do:** Create a histogram of Sales and a boxplot of Profit to understand data distributions.

**How to do it:**

- Step 1: Load your cleaned data and import libraries:
  ```python
  import pandas as pd
  import matplotlib.pyplot as plt
  import seaborn as sns

  df = pd.read_csv('superstore_cleaned.csv')
  df['Order Date'] = pd.to_datetime(df['Order Date'])
  ```

- Step 2: Create the Sales histogram:
  ```python
  plt.figure(figsize=(10, 6))
  plt.hist(df['Sales'], bins=50, color='steelblue', edgecolor='black')
  plt.title('Distribution of Sales')
  plt.xlabel('Sales ($)')
  plt.ylabel('Frequency')
  plt.show()
  ```

- Step 3: Create the Profit boxplot:
  ```python
  plt.figure(figsize=(10, 6))
  sns.boxplot(x=df['Profit'], color='lightcoral')
  plt.title('Boxplot of Profit')
  plt.xlabel('Profit ($)')
  plt.show()
  ```

- Step 4: In a text cell below each plot, write 2-3 sentences: Is the Sales distribution normal, skewed, or bimodal? Where are the quartiles in the Profit boxplot? Are there visible outliers?

**Tool:** `plt.hist()` for histograms, `sns.boxplot()` for boxplots

**Help if stuck:** StatQuest — Histograms (explains when and how to use them): https://statquest.org/video-index/

**Slide reference:** This was covered in Class 3, Slide: "Histogram — Distribution of a Single Variable"

### Task 2: Category-Wise Trends
**What to do:** Create bar charts showing total sales by Category and by Region.

**How to do it:**

- Step 1: Calculate totals by category:
  ```python
  cat_sales = df.groupby('Category')['Sales'].sum().sort_values(ascending=False)
  ```

- Step 2: Plot the bar chart:
  ```python
  plt.figure(figsize=(8, 5))
  cat_sales.plot(kind='bar', color=['#2196F3', '#4CAF50', '#FF9800'])
  plt.title('Total Sales by Category')
  plt.xlabel('Category')
  plt.ylabel('Total Sales ($)')
  plt.xticks(rotation=0)
  plt.show()
  ```

- Step 3: Repeat for Region:
  ```python
  region_sales = df.groupby('Region')['Sales'].sum().sort_values(ascending=False)

  plt.figure(figsize=(8, 5))
  region_sales.plot(kind='bar', color='steelblue')
  plt.title('Total Sales by Region')
  plt.xlabel('Region')
  plt.ylabel('Total Sales ($)')
  plt.xticks(rotation=0)
  plt.show()
  ```

- Step 4: Write a text cell answering: Which category and region generate the most revenue? Any surprises?

**Tool:** `df.groupby().sum()`, `.plot(kind='bar')`

**Help if stuck:** Kaggle Data Visualization Course, Lesson 2 "Bar Charts and Heatmaps": https://www.kaggle.com/learn/data-visualization

**Slide reference:** This was covered in Class 3, Slide: "Bar Chart — Comparing Categories"

### Task 3: Outlier Identification
**What to do:** Numerically identify outliers in the Profit column using the IQR method.

**How to do it:**

- Step 1: Calculate Q1, Q3, and IQR:
  ```python
  Q1 = df['Profit'].quantile(0.25)
  Q3 = df['Profit'].quantile(0.75)
  IQR = Q3 - Q1
  print(f"Q1: {Q1:.2f}, Q3: {Q3:.2f}, IQR: {IQR:.2f}")
  ```

- Step 2: Determine outlier boundaries:
  ```python
  lower_bound = Q1 - 1.5 * IQR
  upper_bound = Q3 + 1.5 * IQR
  print(f"Lower bound: {lower_bound:.2f}, Upper bound: {upper_bound:.2f}")
  ```

- Step 3: Count outliers:
  ```python
  outliers = df[(df['Profit'] < lower_bound) | (df['Profit'] > upper_bound)]
  print(f"Number of outliers: {len(outliers)} out of {len(df)} rows ({len(outliers)/len(df)*100:.1f}%)")
  ```

- Step 4: Write a text cell: How many outliers did you find? Are they mostly high-profit or high-loss items?

**Tool:** `df.quantile()`, boolean indexing with `df[condition]`

**Help if stuck:** Python Data Science Handbook, "Outlier Detection": https://jakevdp.github.io/PythonDataScienceHandbook/

**Slide reference:** This was covered in Class 3, Slide: "Box Plot — Spotting Outliers"

### Task 4: Correlation Heatmap
**What to do:** Create a correlation heatmap for all numerical columns and identify the strongest relationships.

**How to do it:**

- Step 1: Select numerical columns and compute correlations:
  ```python
  numerical_cols = df[['Sales', 'Quantity', 'Discount', 'Profit']]
  corr_matrix = numerical_cols.corr()
  print(corr_matrix)
  ```

- Step 2: Plot the heatmap:
  ```python
  plt.figure(figsize=(8, 6))
  sns.heatmap(corr_matrix, annot=True, cmap='coolwarm', center=0, fmt='.2f')
  plt.title('Correlation Heatmap: Sales, Quantity, Discount, Profit')
  plt.show()
  ```

- Step 3: Write a text cell: Which pairs have the strongest positive correlation? Is any relationship negative? What does the Discount-Profit correlation suggest?

**Tool:** `df.corr()`, `sns.heatmap()` with `annot=True`

**Help if stuck:** Kaggle Data Visualization Course, Lesson 2 "Bar Charts and Heatmaps": https://www.kaggle.com/learn/data-visualization

**Slide reference:** This was covered in Class 3, Slide: "Heatmap — Correlation Matrix"

### Task 5: Time-Based Trend
**What to do:** Plot monthly total sales over time as a line chart and look for seasonal patterns.

**How to do it:**

- Step 1: Resample data to monthly totals:
  ```python
  monthly_sales = df.resample('M', on='Order Date')['Sales'].sum()
  ```

- Step 2: Plot the line chart:
  ```python
  plt.figure(figsize=(12, 6))
  monthly_sales.plot(color='steelblue', linewidth=2)
  plt.title('Monthly Total Sales Over Time')
  plt.xlabel('Month')
  plt.ylabel('Total Sales ($)')
  plt.grid(True, alpha=0.3)
  plt.show()
  ```

- Step 3: Write a text cell: Are there seasonal patterns? Spikes at end of year (holiday shopping)? Any notable dips?

**Tool:** `df.resample('M', on='column')` — resamples time series data to monthly frequency

**Help if stuck:** Kaggle Data Visualization Course, Lesson 3 "Line Charts": https://www.kaggle.com/learn/data-visualization

**Slide reference:** This was covered in Class 3, Slide: "Line Chart — Trends Over Time"

### Task 6: Reflection Questions
**What to do:** Answer these 4 questions in text cells within your notebook (2-3 sentences each).

1. The Sales histogram is likely right-skewed. Why does this happen in retail data?
2. You found outliers in Profit. Should you remove them? What information might you lose?
3. If Discount and Profit have a negative correlation, does that mean the company should stop giving discounts?
4. Looking at your monthly sales trend — if you were a store manager, what action would you take based on this chart?

**Tool:** Markdown cells in Colab (click "+ Text")

**Slide reference:** This was covered in Class 3, Slide: "Correlation — Not Causation"

## Tips
- Always label your axes. A chart with "0, 1, 2, 3" on the x-axis tells nobody anything.
- Use `figsize=(10, 6)` or similar — default Matplotlib sizes are often too small.
- For bar charts, sort the bars by value (descending) so the ranking is immediately obvious.
- Seaborn's `heatmap()` with `annot=True` prints correlation values on the cells. Always use it.
- Correlation is not causation. A strong correlation between Discount and Profit does not mean discounts cause profit changes.

## Submission
- Google Colab notebook with all visualizations rendered (not just code — the plots must be visible).
- Written interpretation for each plot: 2-3 sentences explaining what the chart shows and what it means.
- All charts must have titles, axis labels, and appropriate formatting.
- Due before the next class.
