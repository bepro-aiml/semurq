# Module 2 | Class 2 Assignment: Python Data Preparation Lab

**Related slides:** Class 2 — NumPy and Pandas
**Estimated time:** 60 minutes

## Objective
Load, inspect, clean, and prepare a structured retail dataset using Python and Pandas. This is the bread and butter of any data role — you will do this hundreds of times in your career, so build the muscle memory now.

## What You Need
- Tool: Google Colab: https://colab.research.google.com
- Dataset: Superstore Sales Dataset — download from https://www.kaggle.com/datasets/vivek468/superstore-dataset-final
- Libraries: `pandas` (pre-installed in Colab, no install needed)

## Tasks

### Task 1: Import and Load the Data
**What to do:** Upload the Superstore CSV to Colab and load it into a Pandas DataFrame.

**How to do it:**

- Step 1: Download `SampleSuperstore.csv` from the Kaggle link above (click "Download" on the dataset page).
- Step 2: In Google Colab, click the folder icon on the left sidebar, then click the upload icon (arrow pointing up) and upload the CSV file.
- Step 3: Load the data:
  ```python
  import pandas as pd
  df = pd.read_csv('SampleSuperstore.csv')
  df.head(10)
  ```

- Step 4: Verify you see a table with columns like Ship Mode, Segment, Country, City, State, etc.

**Tool:** `pd.read_csv()` — reads a CSV file into a DataFrame

**Help if stuck:** Kaggle Pandas Course, Lesson 1 "Creating, Reading, Writing": https://www.kaggle.com/learn/pandas

**Slide reference:** This was covered in Class 2, Slide: "Load, Inspect, Understand"

### Task 2: Inspect the Structure
**What to do:** Examine the dataset's shape, data types, missing values, and summary statistics.

**How to do it:**

- Step 1: Check the shape:
  ```python
  print("Shape:", df.shape)  # (rows, columns)
  ```

- Step 2: Run `.info()` to see data types and non-null counts:
  ```python
  df.info()
  ```

- Step 3: Run `.describe()` for summary statistics of numerical columns:
  ```python
  df.describe()
  ```

- Step 4: Check for missing values:
  ```python
  print(df.isnull().sum())
  ```

- Step 5: Add a markdown cell (click "+ Text" in Colab) and write 2-3 sentences describing what you found: how many rows/columns, which columns have missing values, anything surprising in the statistics.

**Tool:** `df.shape`, `df.info()`, `df.describe()`, `df.isnull().sum()`

**Help if stuck:** Python Data Science Handbook, Chapter 3 "Data Manipulation with Pandas": https://jakevdp.github.io/PythonDataScienceHandbook/

**Slide reference:** This was covered in Class 2, Slide: "Pandas — Data Analysis Made Simple"

### Task 3: Handle Missing Values
**What to do:** Fill missing values in numerical columns with median and categorical columns with mode.

**How to do it:**

- Step 1: Identify which columns have missing values (from Task 2).
- Step 2: For numerical columns, fill with median (more robust than mean if outliers exist):
  ```python
  # Example for a numerical column
  df['Sales'].fillna(df['Sales'].median(), inplace=True)
  ```

- Step 3: For categorical columns, fill with mode (most frequent value):
  ```python
  # Example for a categorical column
  df['Ship Mode'].fillna(df['Ship Mode'].mode()[0], inplace=True)
  ```

- Step 4: Verify no missing values remain:
  ```python
  print(df.isnull().sum())
  ```

- Step 5: Add a text cell documenting how many values you filled and in which columns. If no missing values exist, state that explicitly.

**Tool:** `df.fillna()`, `df.median()`, `df.mode()`

**Help if stuck:** Kaggle Pandas Course, Lesson 5 "Summary Functions and Maps": https://www.kaggle.com/learn/pandas

**Slide reference:** This was covered in Class 2, Slide: "Load, Inspect, Understand"

### Task 4: Remove Duplicates
**What to do:** Check for and remove duplicate rows.

**How to do it:**

- Step 1: Count duplicates:
  ```python
  print("Duplicate rows:", df.duplicated().sum())
  ```

- Step 2: Remove them:
  ```python
  df = df.drop_duplicates()
  print("Shape after removing duplicates:", df.shape)
  ```

- Step 3: Add a text cell reporting how many duplicates were found and removed.

**Tool:** `df.duplicated()`, `df.drop_duplicates()`

**Help if stuck:** Kaggle Pandas Course: https://www.kaggle.com/learn/pandas

**Slide reference:** This was covered in Class 2, Slide: "Load, Inspect, Understand"

### Task 5: Convert Data Types
**What to do:** Convert Order Date and Ship Date columns to proper datetime format.

**How to do it:**

- Step 1: Convert the date columns:
  ```python
  df['Order Date'] = pd.to_datetime(df['Order Date'])
  df['Ship Date'] = pd.to_datetime(df['Ship Date'])
  ```

- Step 2: Verify the conversion:
  ```python
  print(df.dtypes)
  ```

- Step 3: Spot-check a few values to make sure dates look correct:
  ```python
  df[['Order Date', 'Ship Date']].head()
  ```

**Tool:** `pd.to_datetime()` — converts strings to datetime objects

**Help if stuck:** Python Data Science Handbook, "Working with Time Series": https://jakevdp.github.io/PythonDataScienceHandbook/

**Slide reference:** This was covered in Class 2, Slide: "Pandas — Data Analysis Made Simple"

### Task 6: Create Derived Features
**What to do:** Create three customer-level summary metrics: total spending, order frequency, and average order value.

**How to do it:**

- Step 1: Calculate total spending per customer:
  ```python
  total_spending = df.groupby('Customer ID')['Sales'].sum().reset_index()
  total_spending.columns = ['Customer ID', 'total_spending']
  ```

- Step 2: Calculate order frequency (number of orders per customer):
  ```python
  order_freq = df.groupby('Customer ID')['Order ID'].nunique().reset_index()
  order_freq.columns = ['Customer ID', 'order_frequency']
  ```

- Step 3: Calculate average order value:
  ```python
  avg_order = df.groupby('Customer ID')['Sales'].mean().reset_index()
  avg_order.columns = ['Customer ID', 'avg_order_value']
  ```

- Step 4: Merge all into a customer summary table:
  ```python
  customer_summary = total_spending.merge(order_freq, on='Customer ID').merge(avg_order, on='Customer ID')
  customer_summary.head(10)
  ```

- Step 5: Save the cleaned DataFrame:
  ```python
  df.to_csv('superstore_cleaned.csv', index=False)
  customer_summary.to_csv('customer_summary.csv', index=False)
  ```

**Tool:** `df.groupby()`, `.sum()`, `.nunique()`, `.mean()`, `.merge()`, `.to_csv()`

**Help if stuck:** Kaggle Pandas Course, Lesson 4 "Grouping and Sorting": https://www.kaggle.com/learn/pandas

**Slide reference:** This was covered in Class 2, Slide: "Vectorized Operations — Math Without Loops"

## Tips
- Always look at your data before doing anything: `df.head()`, `df.info()`, `df.describe()` — run these first, every time.
- When filling missing values, median is more robust than mean if your data has outliers. Check the distribution before deciding.
- `pd.to_datetime()` can fail silently if dates are in unexpected formats. After conversion, spot-check a few values.
- If you get a `SettingWithCopyWarning`, you are probably modifying a slice instead of the original DataFrame. Use `.loc[]` or `.copy()`.
- Save your cleaned DataFrame to a new CSV at the end. Never overwrite the original file.

## Submission
- Google Colab notebook link with all code cells executed and visible output.
- Cleaned dataset saved as `superstore_cleaned.csv` (not overwriting the original).
- Short text explanation (2-3 sentences) after each major step explaining what you did and why.
- Due before the next class.
